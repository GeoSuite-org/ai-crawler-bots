---
name: Feature request
about: New bot, new CLI flag, or new lib export
title: '[feat] '
labels: enhancement
assignees: ''
---

### What would you like the tool to do?

<!-- Plain language. What problem does this solve? -->

### If this is a new bot…

- Bot name (as it appears in `User-agent:` lines):
- Operator (the company that runs it):
- Operator documentation URL that lists this bot's UA string:
- Suggested `purpose` (one of: `training`, `search`, `user-agent`):
- Default `robotsDirective` (`Allow` or `Disallow`):

> We don't accept bot entries sourced only from third-party tracker blogs.
> The User-Agent must come from the operator itself — see
> [CONTRIBUTING.md](../../CONTRIBUTING.md).

### If this is a new CLI flag or library export…

- Proposed CLI signature:
- Sample input → expected output:
- Why the existing flags don't cover this:

### Alternatives considered

<!-- Optional. Other tools / libraries / approaches you tried. -->
